The app shell and styling are a strong start, but the core structure of the Log experience is not aligned with the intended product behavior.

Do not focus on polish right now. Do not rebuild the entire app. Keep the current visual theme, layout direction, header, and bottom navigation. Instead, perform a structural correction pass on the Log tab so each category follows its intended input flow.

IMPORTANT:
This app is not a generic tap-to-log tracker. Each category has its own specific behavior and input structure. Update the Log screen to reflect that.

CORE SYSTEM RULES:
- All data is stored in a central events array
- Each event must follow:
  { id, type, value, day_key, metadata, created_at }
- day_key must follow a 3:00 AM reset rule:
  - The day resets at 3:00 AM
  - Events between 12:00 AM and 2:59 AM belong to the previous day
- Store detailed inputs inside metadata (do not flatten structured data)
- Introduce a mode system:
  - "guest" (current state, local only)
  - "user" (future database mode)

LIMIT RULES:
- Mood: maximum 3 entries per day
- Wins are automatically calculated, never manually entered

Please make the following structural corrections:

1. HYDRATION
- Keep hydration as the quickest interaction
- Maintain +1 and +2 buttons
- Logging hydration should remain immediate

2. MOOD
- Mood should be emoji-only
- Allow up to 3 mood logs per day
- Mood logging should be fast and intentional

3. SLEEP
- Sleep must NOT be a single generic button press
- Build the intended two-stage structure:
  - After 7 PM: allow user to log “Went to bed” time
  - Next day until 7 PM: allow user to complete the sleep log with:
    - time fell asleep
    - time woke up
    - wake mood
- Keep UI simple but structured correctly

4. MEDICATION
- Medication should not be a generic tap-only placeholder
- Prepare structure so medications can later come from user profile/account data
- For now, create a simple but clear medication logging interaction

5. BATHROOM
- Bathroom logging should not be a single generic tap
- Include inputs:
  - duration
  - optional notes
- Keep it simple but structured

6. WIN SYSTEM
- Wins must be tied to meaningful completed actions
- Maintain:
  - Hydration: every 2 cups = 1 win (based on daily total)
  - Mood: 1 win per entry (max 3/day)
  - Medication: 1 win per entry
  - Sleep: 1 win for completed sleep log
  - Bathroom: 1 win per entry

7. INTERACTION SIMPLICITY
- Avoid overcomplicated multi-step flows
- Only sleep should have a multi-stage process
- Other inputs should remain quick and efficient

8. KEEP THE APP SHELL
- Preserve the current autumn styling direction
- Preserve header and bottom navigation
- Do not replace the entire UI

9. DEBUG + STRUCTURE
- Maintain the DEBUG system
- Ensure events, day_key, and win count remain visible in debug panel

GOAL:
Correct the product structure so the Log tab matches the intended real app behavior, especially for Sleep and Bathroom, while preserving the current shell and visual design. The result should be clean, structured, and ready for future database integration.